import React from 'react';

import './Intro.scss';

const intro = ( props ) => (
    <div className="Intro">
      <h1>I'm a Drupal themer and front-end web developer</h1>
    </div>
);

export default intro;
